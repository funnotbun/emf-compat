// Better Combat addon — Minecraft 26.x on Fabric.
//
// 26.x ships deobfuscated, so there is no mappings line and no remap step: plain fabric-loom,
// and the jar task produces the final artefact. See build.remap.gradle for the 1.21.x half.

plugins {
    id 'net.fabricmc.fabric-loom' version '1.17.11'
    id 'maven-publish'
}

apply from: rootProject.file('extensions/better_combat/fabric/versions.gradle')

def mc = ext.mc()

// mod_version has to be an ext property, not just `version`: the root build script reads
// it back with findProperty to name the uploaded jar, and without it the module would
// inherit the 1.0.0 default from the root gradle.properties.
ext.mod_version = '2.1.0'
version = mod_version
group = mod_group_id

ext.mod_id = 'emf_compat_better_combat'
ext.mod_name = 'EMF Compat: Better Combat'
ext.mod_description = 'Restores Better Combat arm poses after EMF animation, so attack animations are not overwritten by Entity Model Features.'
// The root build script reads this to name the uploaded jar; on a flat module it comes
// from gradle.properties, here it is whichever version node is being built.
ext.minecraft_version = stonecutter.current.version

base {
    archivesName = mod_id
}

loom {
    splitEnvironmentSourceSets()

    mods {
        "${mod_id}" {
            sourceSet sourceSets.main
            sourceSet sourceSets.client
        }
    }
}

repositories {
    mavenLocal()
    maven { url = 'https://repo.redlance.org/public' }
    exclusiveContent {
        forRepository {
            maven {
                name = "Modrinth"
                url = "https://api.modrinth.com/maven"
            }
        }
        filter {
            includeGroup "maven.modrinth"
        }
    }
}

dependencies {
    minecraft "com.mojang:minecraft:${stonecutter.current.version}"
    implementation "net.fabricmc:fabric-loader:${mc.loader_version}"
    implementation "net.fabricmc.fabric-api:fabric-api:${mc.fabric_version}"

    clientCompileOnly fabricApi.module("fabric-api-base", mc.fabric_version)

    implementation project(mc.core)

    compileOnly mc.emf
    compileOnly mc.etf
    compileOnly mc.betterCombat
    compileOnly mc.pal
}

processResources {
    // The addon ships its own copies of two Better Combat animations.
    duplicatesStrategy = DuplicatesStrategy.INCLUDE

    def props = [
            "version"          : version,
            "minecraft_version": stonecutter.current.version,
            "loader_version"   : mc.loader_version,
            "mod_id"           : mod_id,
            "mod_name"         : mod_name,
            "mod_description"  : mod_description,
            "mod_authors"      : mod_authors,
            "mod_license"      : mod_license,
    ]
    props.each { k, v -> inputs.property(k, v) }
    filteringCharset = "UTF-8"

    filesMatching("fabric.mod.json") {
        expand(props)
    }
}

def targetJavaVersion = mc.java
tasks.withType(JavaCompile).configureEach {
    it.options.encoding = "UTF-8"
    it.options.release.set(targetJavaVersion)
}

java {
    def javaVersion = JavaVersion.toVersion(targetJavaVersion)
    if (JavaVersion.current() < javaVersion) {
        toolchain.languageVersion = JavaLanguageVersion.of(targetJavaVersion)
    }
    withSourcesJar()
}

jar {
    from(rootProject.file("LICENSE")) {
        rename { "${it}_${mod_id}" }
    }
}

publishing {
    publications {
        create("mavenJava", MavenPublication) {
            artifactId = mod_id
            from components.java
        }
    }
}
